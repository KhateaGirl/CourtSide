Create a modern Flutter UI screen following Material 3 design.

Requirements:
- Keep UI separate from business logic
- Use a clean and minimal layout
- Support responsive design
- Use reusable widgets where possible
- Use clear spacing and typography
- Handle loading, empty, and error states

Architecture rules:
- UI only handles presentation
- Services handle business logic
- Repository handles database calls

Output:
- complete Flutter widget
- reusable components extracted if necessary
